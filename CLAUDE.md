# Aura — Claude Code 工作规则

> 本文件在每个 session 启动时自动加载。遇到架构决策先查这里，文件里有答案就直接按这里做，不要停下来问用户。

---

## 一、已定的架构决策（不需要再讨论）

### 不做的功能
- Metrics 内置端点（用户自己加 `/metrics` 路由 + micrometer）
- 分布式限流（交给 nginx / 网关层）
- ORM、AOP、大型 IoC 容器（破坏轻量定位）
- LLM Client 封装（aura-ai 模块，暂不在计划内）
- 数据库 Migration 工具（暂不在计划内）

### 已定的设计原则
- **万物皆 service**：不单独设 `controllers()` 方法，有 `@Path` 注解的 service 就是路由注册点
- **core 模块不引入 fastjson2**：JWT payload 格式自控，不需要通用 JSON 库
- **`scan()` 标记 `@Deprecated` 但不删除**：保持向后兼容
- **TestClient 不启动 HTTP server**：内存路由，零依赖，毫秒级测试
- **单构造器约定**：Service 只允许一个 public 构造器，IoC 按类型注入

### 当前版本状态
- 最新稳定版：v0.6.2（已发布 Maven Central）
- dev 分支：包含 fix（连接泄漏、MCP 异常处理、线程安全、URL 编码），未发布
- Backlog：ExceptionSnapshot（app.onException 异常快照），已设计未实现

---

## 二、完成前必须做的验证（不能跳过）

代码修改完成后，按顺序执行：

```bash
# 1. 全局搜索同类代码，确认没有遗漏相同逻辑的地方
grep -r "改动相关的关键词" src/

# 2. 跑全量测试，必须全部通过才算完成
cd /root/aura && mvn test

# 3. 测试通过后才能说"完成了"
```

**严禁：** 在测试失败的情况下说"完成了，但有几个测试没过"。

---

## 三、不确定时的处理方式

遇到以下情况，必须先列假设，不能直接给结论：
- 没有直接证据的根因分析
- 涉及外部服务/网络的问题排查
- 需要多次验证才能确认的猜测

**正确格式：**
> 我不确定，可能是以下原因之一：
> A. xxx（可以通过 yyy 验证）
> B. xxx（可以通过 yyy 验证）
> 建议先验证 A，因为 xxx。

**不能这样：** 直接给出"原因是 xxx"，然后被用户追问才承认"没有证据"。

---

## 四、跨文件改动清单

以下几类改动历史上容易漏，每次遇到必须主动搜索：

| 改动类型 | 必须搜索的地方 |
|---------|--------------|
| 字段访问方式变更（如去掉 `toLowerCase()`） | `grep -r "toLowerCase" src/` |
| 连接/资源管理模式 | 搜索同类 `getConnection()` 调用 |
| 异常处理模式 | 搜索同类 catch 块 |
| 枚举/常量变更 | 搜索所有引用点 |

---

## 五、架构决策升级流程

遇到 **文件里没有记录的** 架构决策：
1. 列出选项 + 各自的权衡
2. 给出你的建议（说明理由）
3. 等用户确认
4. 决定后把结论追加到本文件的第一节

---

## 六、Session 续接说明

如果是续接之前的 session（`--resume` 或 `-c`）：
- 先用 `git log --oneline -5` 确认当前代码状态
- 先用 `mvn test -q` 确认当前测试是否通过
- 再接着做，不要假设上次的状态还在
